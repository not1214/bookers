<h1>Editing Book</h1>
<%= form_with model:@book, url:book_path(@book.id), local:true do |f| %>

  <p>Title</p>
  <%= f.text_field :title%>

  <P>Body</P>
  <%= f.text_area :body %>

  <%= f.submit "Update Book" %>
<% end %>